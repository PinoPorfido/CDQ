@model CDQ.Models.Helper.HelpCausaDebito

@{
    ViewData["Title"] = "Causa Indebitamento";
}

<br />
<br />

<div class="columns">
    <div class="column is-offset-3 is-1">
        <a asp-action="CauseDebito" asp-route-Ins="true" class="button back">
            <span class="icon">
                <i class="fas fa-backward"></i>
            </span>
        </a>
    </div>
    <div class="column is-4" align="center">
        <h1 class="title is-4">@(Model.Del ? "Elimina" : Model.Ins ? "Inserisci" : "Aggiorna") Causa Indebitamento</h1>
    </div>
</div>

<div class="column is-6 is-offset-3">
    <div class="panel">
        <div class="panel-heading">Causa Indebitamento</div>
        <div class="panel-block">
            <div class="container">
                <form method="post">

                    <div class="column is-full">

                        @Html.HiddenFor(model => Model.CausaDebito.ID)
                        @Html.HiddenFor(model => Model.CausaDebito.IsSistema)
                        @Html.HiddenFor(model => Model.CausaDebito.User)

                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                        <div class="control is-expanded">
                            <div class="field">
                                <input asp-for="CausaDebito.Descrizione" readonly="@Model.Del" ? true : false" class="input" />
                                @Html.ValidationMessageFor(m => m.CausaDebito.Descrizione, "", new { @class = "help help-message" })
                            </div>
                        </div>
                    </div>

                    <div class="column is-full">

                        <button type="submit" class="button is-dark">@(Model.Del ? "Elimina" : Model.Ins ? "Inserisci" : "Aggiorna")</button>

                    </div>

                </form>               
            </div>
        </div>
    </div>
</div>
